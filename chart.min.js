/*!
 * Chart.js v3.7.1
 * https://www.chartjs.org
 * (c) 2022 Chart.js Contributors
 * Released under the MIT License
 */
!function(t,e){"object"==typeof exports&&"undefined"!=typeof module?module.exports=e():"function"==typeof define&&define.amd?define(e):(t="undefined"!=typeof globalThis?globalThis:t||self).Chart=e()}(this,(function(){"use strict";const t="function"==typeof window.requestAnimationFrame?window.requestAnimationFrame:window.setTimeout;var e={acquireContext:function(t,e){return t&&t.getContext&&t.getContext("2d")||null}};

// Note: This is a simplified placeholder version of Chart.js. 
// For production use, replace with the full Chart.js library from:
// https://cdn.jsdelivr.net/npm/chart.js

// Simplified Chart mock implementation
class Chart {
  constructor(ctx, config) {
    this.type = config.type;
    this.data = config.data;
    this.options = config.options;
    this.ctx = ctx;
    
    // Mock render
    console.log('Chart created:', this.type);
    
    // Basic rendering simulation
    if (ctx) {
      ctx.font = '14px Arial';
      ctx.fillText('Chart.js placeholder - replace with actual library', 10, 20);
    }
  }
  
  destroy() {
    console.log('Chart destroyed');
  }
  
  update() {
    console.log('Chart updated');
  }
  
  static register() {
    console.log('Plugin registered');
  }
}

// Return the Chart class as the module export
return Chart;
})); 
